server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      discovery:
        enabled: true
      server:
        native:
          search-locations: D:\Program Files\JA\ms-config-properties\config-client\dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/


